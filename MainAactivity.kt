         bottomBar = {
                                BottomAppBar {

                                }
                        NavigationBar {
                            IconButton(onClick = {  }) {
                                Icon(Icons.Rounded.Home, contentDescription ="home", modifier = Modifier.size(200.dp) )

                            }
                            IconButton(onClick = {  }) {
                                Icon(Icons.Rounded.Favorite, contentDescription ="fav", modifier = Modifier.fillMaxWidth().size(200.dp) )

                            }
                            IconButton(onClick = {  }) {
                                Icon(Icons.Rounded.Info, contentDescription ="info", modifier = Modifier.fillMaxWidth().size(200.dp) )

                            }
                            IconButton(onClick = {  }) {
                                Icon(Icons.Rounded.Settings, contentDescription ="setting ", modifier = Modifier.fillMaxWidth().size(200.dp) )

                            }


                        }
                    },
